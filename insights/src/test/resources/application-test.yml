server:
  port: 0

spring:
  main:
    allow-bean-definition-overriding: true
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

insights:
  hub-url: http://localhost:1
  timezone: America/Argentina/Buenos_Aires
  baseline-min-days: 7
  rollup:
    enabled: false
