server:
  port: 8090

spring:
  application:
    name: event-bridge

management:
  endpoints:
    web:
      exposure:
        include: health,info

nats:
  url: nats://localhost:4222

bridge:
  nats:
    durable: bridge

logging:
  level:
    com.hub.bridge: INFO
